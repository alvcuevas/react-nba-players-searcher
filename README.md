## NBA Players searcher built using React, Redux & Saga

### Resources

- React 16.8
- React-redux 6
- Redux-saga
- Styled-components

### Usage

1. Install dependencies: `yarn install` | `npm install`
2. Run application on browser: `yarn start` | `npm start`
